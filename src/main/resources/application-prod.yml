spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pong-clash
    username: dev
    password: root

  jpa:
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
    hibernate:
      ddl-auto: update
      show-sql: true
jwt:
  secret: MYJWTSECRET